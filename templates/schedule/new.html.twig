{% extends 'base.html.twig' %}

{% block title %}Ajouter un horaire{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="display-6">Ajouter un horaire</h1>

        {{ include('schedule/_form.html.twig') }}

        <a href="{{ path('app_schedule_index', {'id': bakery.id}) }}"><button type="button" class="btn btn-secondary">Retour à la liste</button></a>
    </div>
{% endblock %}
